services:
  postgres:
    image: postgres:15-alpine
    container_name: study_hub_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: gfhjkm
      POSTGRES_DB: smart_study_hub
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: